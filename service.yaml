apiVersion: v1
kind: Service
metadata:
  name: n8n-main
  namespace: n8n
spec:
  clusterIP: None   # headless (required for StatefulSet serviceName)
  selector:
    app: n8n-main
  ports:
    - name: http
      port: 80
      targetPort: 5678
